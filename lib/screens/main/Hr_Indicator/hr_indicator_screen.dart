import 'package:flutter/material.dart';

class HRIndicatorScreen extends StatelessWidget {
  const HRIndicatorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('HRIndicatorScreen Fragment ! '),
    );
  }
}
